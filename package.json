{
  "name": "the-delivery-desk",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node generate-pages.js",
    "check": "node --check script.js && node --check generate-pages.js && node audit-site.js"
  }
}
